# 更新日志

## v0.3.0

本版本修复了 6 个影响推送可靠性的 bug,并新增 6 项功能。建议所有用户升级。

### 新增功能

1. **观测期史低展示** —— `/sw list` 与 `/sw export` 中会显示每个游戏观测到的最低价,便于快速判断当前折扣是否值得入手。
2. **愿望单来源管理** —— 新增 `/sw sources` 查看已导入的愿望单来源(含各来源关联的游戏数),`/sw unlink <序号> [all]` 按来源解除,加 `all` 可连带移除由该来源导入的游戏。
3. **发售提醒** —— 监控中的未发售游戏发售后推送一次提醒;若发售时正在打折,会一并带上折扣信息。可用 `enable_release_notify` 关闭。
4. **权限控制** —— 新增 `admin_only` 配置,开启后 `bind`/`unbind`/`add`/`remove`/`import`/`sync`/`unlink`/`check` 仅群管理员或 AstrBot 管理员可用,查询类命令(`list`/`history`/`export`/`sources`/`status`)不受限制。
5. **导出清单** —— 新增 `/sw export`,把监控列表导出为 Markdown 表格(含现价、折扣、观测史低),便于分享或存档。
6. **价格走势** —— 新增 `/sw history <链接或AppID>`,查看某游戏最近 15 个价格观测点与观测期最低价。

### Bug 修复

1. **导入愿望单会吞掉已监控游戏的降价**(严重)
   导入愿望单时为新增游戏建立价格基线,基线流程会把当时的现价写入"已推送"记录。若愿望单里存在已在监控中的游戏,它随后真实发生的降价会被判定为"已提醒过"而永久静默丢失。
   现在基线阶段(`record_push=False`)不再写入推送记录,并只对本次真正新增的游戏建立基线。

2. **单个游戏请求超时中断整轮检查**(严重)
   网络层的超时(`asyncio.TimeoutError`)与连接异常未统一捕获,一个游戏超时会让本轮剩余游戏全部跳过,检查提前结束。
   现在所有网络层异常统一转为可捕获的 `SteamAPIError`,仅计为该游戏一次失败,其余游戏继续检查。

3. **`/sw list` 价格显示错误**
   无折扣但有价格的游戏会误显示"暂无价格数据"。已修正判断顺序,优先展示现价。

4. **`enable_auto_check` 关闭后仍在轮询**
   关闭开关不生效(必须重启插件)。现在轮询任务会周期性复查配置:关闭时进入休眠且不发起任何 Steam 请求,在 WebUI 重新打开开关后无需重启即可恢复自动检查。

5. **推送文案硬编码 `¥`**
   非国区(如 `us`/`jp`)会显示错误的货币符号。现在所有金额统一按 `region` 对应的货币符号渲染(如 cn → `¥35`、us → `$50`),列表、推送、导出、历史全部一致。

6. **自定义 URL 解析绕过节流**
   `resolve_vanity` 未走统一的节流通道,可能触发 Steam 限流。现已纳入节流,并把 404 映射为"找不到自定义URL对应的用户"的友好提示。

### 兼容性

- v0.2.0 的 `wishlist_sources` 旧数组格式会在加载时自动升级为字典结构,无需手动迁移;
- 旧的价格状态数据可继续使用,新增的史低与历史字段会在后续检查中自动补齐。

### 升级方式

```bash
cd AstrBot/data/plugins/astrbot_plugin_steam_wishlist
git pull
```

或直接在 AstrBot WebUI 插件管理页更新,然后重启 AstrBot。